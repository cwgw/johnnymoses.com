{
  "name": "@johnnymoses.com/johnnymoses.com",
  "private": true,
  "description": "The website of Native American storyteller Johnny Moses",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --parallel",
    "build:studio": "yarn && (cd packages/studio && yarn run build)",
    "build:web": "yarn && (cd packages/studio && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN yarn run graphql-deploy) && (cd packages/web && yarn run build)",
    "develop": "lerna run develop --parallel",
    "format": "lerna run format --parallel",
    "graphql-deploy": "lerna run graphql-deploy"
  },
  "devDependencies": {
    "husky": "^4.2.5"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cwgw/johnnymoses.com.git"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit --since HEAD --exclude-dependents"
    }
  }
}
