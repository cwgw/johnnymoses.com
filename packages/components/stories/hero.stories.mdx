import { Meta, Preview, Story } from '@storybook/addon-docs/blocks'

import { SanityData } from './utils/sanityData'
import Hero from '../src/components/page-blocks/hero'

<Meta title="Page Blocks | Hero" component={Hero} />

# Hero

<Preview>
  <Story name="Default">
    <SanityData module='heroModule' >
      {moduleData => (
        <Hero {...moduleData} />
      )}
    </SanityData>
  </Story>
</Preview>
