import { Meta, Preview, Story } from '@storybook/addon-docs/blocks'

import { SanityData } from './utils/sanityData'
import ProductGrid from '../src/components/page-blocks/productGrid'

<Meta title="Page Blocks | Product Grid" component={ProductGrid} />

# ProductGrid

<Preview>
  <Story name="Default">
    <SanityData module='productGridModule' >
      {moduleData => (
        <ProductGrid {...moduleData} />
      )}
    </SanityData>
  </Story>
</Preview>
