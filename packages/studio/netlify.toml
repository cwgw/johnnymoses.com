[build]
  base = "/"
  command = "npm run build:studio && cp packages/studio/netlify.toml packages/studio/dist && cp packages/studio/static/*.html packages/studio/dist"
  publish = "/packages/studio/dist"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
